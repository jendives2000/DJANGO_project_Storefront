from django.shortcuts import render
from django.http import HttpResponse

# URL = 127.0.0.1:8000/playground/...

# 1. Create a view here (a function that handles an HTTP request)
# 2. then map it to the urls.py : this will make the function being called when the requested URL is actually requested.
